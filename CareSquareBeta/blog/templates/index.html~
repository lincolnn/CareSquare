<!DOCTYPE html>
 
<html lang="en">
 
<head>
    <meta charset="utf-8" />
    <link href="../../../media/css/style.css" rel="stylesheet">
    <link href="assets/css/bootstrap.css" rel="stylesheet">
    <link href="assets/css/carousel.css" rel="stylesheet">
    <link href="images/favicon.ico" rel="shortcut icon">
    <title>CareSquare Beta Front Page</title>
</head>
 
<body>
 
<div class="container">
    <div class="userauthentication">
        {% if not user.is_authenticated %}<a href="/accounts/login/?next=/forum/">login</a> | <a
            href="/accounts/register/">register</a>{% endif %}

        {% if user.is_authenticated %}<a href="/accounts/logout/?next=/forum/">logout</a>
            {% endif %}
    </div>

    <h1>CareSquare News</h1>
    <hr />
    {% for post in posts %}
        <div class="post">
        <h2>{{ post.title }}</h2>
        <h3>Posted on {{ post.timestamp }} by {{ post.author }}</h3>
        <p>{{ post.bodytext }}</p>
        </div>
        <hr />
    {% endfor %}
</div>
 
</body>
 
</html>
